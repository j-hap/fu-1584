This file only exist, so that is has en_US in its name.
The Settings of the program look for that extension to determine
the configurable languages. Since en_US is the default
language, the useable .properties files for that language
do not have a language specific extension, therefore that
language would be missing in in the settings dialog.